import { describe, test, expect } from "vitest";

describe("index.test.ts", function () {
    //before(async () => { });

    //beforeEach(async () => { });

    test("true", async () => {
        expect(true).toBe(true);
    });
});
